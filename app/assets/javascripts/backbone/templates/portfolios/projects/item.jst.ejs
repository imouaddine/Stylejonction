<figure class="project_cover">
        <% if(cover && cover.image.display) { %>
                <img src="<%= (cover.image.display.url) %>?timestamp=<%= new Date().getTime() %>" alt="<%= title %>" ></img>
        <% } %>
        <figurecaption class="title">
                <%= title %>
        </figurecaption>
</figure