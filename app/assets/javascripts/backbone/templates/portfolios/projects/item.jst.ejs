<figure class="project_cover">
	<% if(cover && cover.image.thumb) { %>
		<img src="<%= (cover.image.thumb.url) %>?timestamp=<%= new Date().getTime() %>" alt="<%= title %>" ></img>
	<% } %>
	<figurecaption class="title">
		<%= title %>
	</figurecaption>
</figure>