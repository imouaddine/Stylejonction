<figure class="project_cover">
	<% if(cover.image.thumb) { %>
		<img src="<%= (cover.image.thumb.url) %>" alt="<%= title %>" ></img>
	<% } %>
	<figurecaption class="title">
		<%= title %>
	</figurecaption>
</figure>