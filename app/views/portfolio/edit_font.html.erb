<%= render('sidebar') %>
<section id="edit">
	<%= form_for(@portfolio) do |f| %>
		<div class="field">
		   <%= f.label :font %><br />
		   <%= f.text_field :font %>
		 </div>
	<% end %>
</section>

<%= render  "preview",  :action => 'edit_portfolio'  %>
