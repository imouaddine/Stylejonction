<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @project.title %>
	<%= image_tag @project.cover.url(:thumb) if @project.cover? %>
</p>


<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
