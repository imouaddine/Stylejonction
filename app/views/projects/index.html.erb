<h1>Listing projects</h1>

<table>
  <tr>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @projects.each do |project| %>
  <tr>
    <td><%= project.title %></td>
    <td><%= link_to 'Show', portfolio_project_path(@portfolio, project) %></td>
    <td><%= link_to 'Edit', edit_portfolio_project_path(@portfolio, project) %></td>
    <td><%= link_to 'Destroy', portfolio_project_path(@portfolio, project) , confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />


