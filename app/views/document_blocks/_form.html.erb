<%= form_for(@document_block) do |f| %>
  <% if @document_block.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@document_block.errors.count, "error") %> prohibited this document_block from being saved:</h2>

      <ul>
      <% @document_block.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
