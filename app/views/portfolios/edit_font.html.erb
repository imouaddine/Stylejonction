<%= render('sidebar') %>
<section id="edit">
<%= form_for(@portfolio, :url => portfolio_path) do |f| %>
  <%= select "font", "id", @fonts.map {|u| [u.name,u.id]} %>
  <%= submit_tag("Change font") %>
<% end %>
</section>
<% content_for :preview do %>
  <%= render  "preview",  :action => 'edit_portfolio'  %>
<% end %>

