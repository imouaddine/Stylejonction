<%= form_for(@portfolio, :url => portfolio_index_path, :html => { :method => :post }) do |f| %>

  <% if @portfolio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@portfolio.errors.count, "error") %> prohibited this portfolio from being saved:</h2>
      <ul>
      <% @portfolio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :layout %><br />
    <%= f.text_field :layout %>
  </div>

  <div class="field">
    <%= f.label :theme %><br />
    <%= f.text_field :theme %>
  </div>

  <div class="field">
    <%= f.label :font %><br />
    <%= f.text_field :font %>
  </div>

  <%= submit_tag 'Create' %>
<% end %>


